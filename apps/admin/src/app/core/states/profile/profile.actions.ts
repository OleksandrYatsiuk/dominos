export class FetchProfile {
    static readonly type = '[Profile] fetch data about authenticated user';
}

export class Logout {
    static readonly type = '[Profile] log out authenticated user form the platform';
}